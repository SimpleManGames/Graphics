#version 450

in vec4 vColor;
in vec4 vPosition;
out vec4 outColor;

uniform float time = 0.0f;

void main() { 
	outColor = vColor;
	outColor.r = (sin(time) + 1.0 / 2.0);
	outColor.b = (sin(time) + 1.0 / 2.0);
	outColor.g = (cos(sin(time) + 1.0) / 2.0);
}